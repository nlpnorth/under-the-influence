# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
.eggs/
*.egg-info/
*.egg

# ── Environments ─────────────────────────────────────────────────────────────
# .venv embeds absolute paths from the machine that created it — never commit it.
.venv/
venv/
env/
ENV/
.python-version

# ── Secrets and local configuration ──────────────────────────────────────────
# .env holds HF_TOKEN and any WIKIDATA_CONTACT address.
.env
.env.*
!.env.example
*.pem
*.key

# ── Type checkers, linters, test runners ─────────────────────────────────────
.mypy_cache/
.ruff_cache/
.pytest_cache/
.cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# ── Editors and OS ───────────────────────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~
.claude/

# ── Notebooks ────────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# =============================================================================
# Pipeline outputs and data
#
# Everything below is either regenerated by the run/ scripts or is corpus data
# that cannot be redistributed. The repository holds code and configuration
# only; see README §Data for how to obtain the corpora.
# =============================================================================

# ── Corpora and derived corpus files ─────────────────────────────────────────
data/wikipedia/
data/pickles/
data/filter_output/
data/bear_facts_filter_output/
# BLiMP paradigm files, downloaded on first use by probe_models.py
data/blimp/
*.conllu
*.conllu.gz

# ── Training artifacts ───────────────────────────────────────────────────────
artifacts/
raw_text/
tokenized_data/
shuffled_tokenized_data/
tokenized_data_split/
checkpoint-*/
*.safetensors
*.bin
*.pt
*.ckpt

# ── Attribution artifacts ────────────────────────────────────────────────────
# Gradient indices and score memmaps are large and fully regenerable.
gradients/
query_index/
kfac_hessian/
*.npy
*.memmap
delta_I_stats.npz
top_inspect.parquet
prec_at_k.json

# ── Measurement and logs ─────────────────────────────────────────────────────
codecarbon/
emissions*.csv
slurm_outputs/
*.out
*.err
slurm-*.out

# ── Scratch ──────────────────────────────────────────────────────────────────
scratch/
tmp/
.tmp_*/
*.prof
